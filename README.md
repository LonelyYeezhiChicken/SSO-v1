# SSO-v1

第一版SSO

## docker

### docker build image

```shell
docker build -t nest-app .
```

### run in docker container

```shell
docker run -p 80:3050 nest-app 
```

## docker compose

### build docker compose

```shell
docker-compose up
```
